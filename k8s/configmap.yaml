kind: ConfigMap
apiVersion: v1
metadata:
  name: golang-configmap
  namespace: default
  labels:
    app: golang-configmap
    name: golang-configmap
data:
  .env: |
    PORT=3000
    DB_HOST=mysql
    DB_PORT=3306
    DB_NAME=db_golang
    DB_USER=root
    DB_PASSWORD=root
